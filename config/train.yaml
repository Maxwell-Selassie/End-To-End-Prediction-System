model:
  name: random_forest
  params:
    n_estimators: 100
    max_depth: 10
    random_state: 42

  evaluation:
    primary_metric: f1
    threshold: 0.5

  training:
    test_size: 0.2
    stratify: true
    